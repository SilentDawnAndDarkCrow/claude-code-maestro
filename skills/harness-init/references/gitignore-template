# =============================================================
# 敏感配置 — 禁止提交真实密钥
# =============================================================
.env
.env.local
.env.*.local

# =============================================================
# ⚠️  Harness 完整性保护
# 以下目录包含 Multi-Agent Harness 的质量保证核心文件：
#   - .claude/agents/          （项目级 agent 定义：test-writer、impl-worker 等）
#   - .claude/hooks/           （hook 脚本：前置门控、质量检查）
#   - .claude/rules/           （规范文件：coding、testing、security）
#
# 禁止将 .claude/ 加入此文件。
# 这些文件必须提交 git，确保修改可追溯、可审查，
# 且 impl-worker 在 isolation:worktree 模式下能访问 hook 脚本。
# =============================================================
